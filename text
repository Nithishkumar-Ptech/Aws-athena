aws athena start-query-execution \
  --query-string "SELECT * FROM ${your_table} limit 10;" \
  --work-group ${your_workgroup} \
  --result-configuration "OutputLocation=s3://${your_bucket}/results/"
